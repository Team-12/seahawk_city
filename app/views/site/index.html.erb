<!-- <div class="container" style="width: 100%"> -->
  <div class="row">
    <div class="col-xs-12 col-md-12">
      <h2>Capture The Flag</h2>
      <!-- map -->
      <div class="mainMap">
        <div id="map"></div>
      </div>
      <!-- end map -->
      <br><%= link_to "Checkin", new_checkin_path, :class => "btn btn-primary", :id => "checkin_link" %>
    </div>
  </div>
<!-- </div> -->

<script>

$(function() {
  <% if @current_user && @current_user.checkins.any? %>
    var mapOptions={
      center:[<%= @last_user_checkin.latitude %>,<%= @last_user_checkin.longitude %>],
      zoom:16,
      markers:<%=raw @hash.to_json %>
    }
  <% elsif @current_user %>
    var mapOptions={
      center:[<%= @user_pin[0] %>,<%= @user_pin[1] %>],
      zoom:16,
      markers:<%=raw @hash.to_json %>
    }
  <% else %>
    var mapOptions={
      center:[47.6097,-122.3331],
      zoom:11,
      markers:<%=raw @hash.to_json %>
    }
  <% end %>


  drawMap(mapOptions);

})

</script>

  <hr>
  <div class="row">
    <div class="col-xs-12 col-md-12">
      <h2>Nearby Events</h2>
      <div id="nearby">
        <ul class="list-group">
          <% @nearby.each do |n| %>
            <li class="list-group-item nearby-event nearEvent"><%= link_to n.date.to_s + ': ' + n.name + ' @ ' + n.location.name, "events/" + n.id.to_s %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>


